using Microsoft.AspNetCore.Identity;

namespace moyo_ismailstarke_backend.Models
{
    public class User : IdentityUser
    {
        // Does not need to be populated because it inherits from IdentityUser
    }
}
